// import { Test, TestingModule } from '@nestjs/testing';

describe('-- User Controller Testings --', () => {
  describe('* Get User by ID', () => {
    it('should return a user if successful', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });

  describe('* Get User Bookings', () => {
    it('should return user bookings if successful', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });

  describe('* Update User', () => {
    it('should update a user if successful', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });

  describe('* Delete User', () => {
    it('should delete a user if successful', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });
});
