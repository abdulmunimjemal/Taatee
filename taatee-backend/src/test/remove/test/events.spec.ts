// import { Test, TestingModule } from '@nestjs/testing';

describe('-- Event Testings --', () => {
  describe('* Get One By Id', () => {
    it('should return an entity of event if successful', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });

  describe('* Create event ', () => {
    it('should return an object of event entity when created', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });

  describe('* Update Event', () => {
    it('should update an event if successful', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });

  describe('* Delete Event', () => {
    it('should delete an event if successful', () => {
      const x = 5;
      const y = 0;
      const expectedResult = 5;
      expect(x + y).toEqual(expectedResult);
    });
  });
});
